/**
 * @fileoverview Re-exports the 'glob-parent' library using require.
 * Extracts the base directory path from a glob pattern.
 * Provides a consistent ESM export interface.
 */
const globParent = require('glob-parent');

export { globParent }; 