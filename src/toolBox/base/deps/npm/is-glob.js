/**
 * @fileoverview Re-exports the 'is-glob' library using require.
 * Checks if a string is likely a glob pattern.
 * Provides a consistent ESM export interface.
 */
const isGlob = require('is-glob');

export { isGlob }; 